{
  "name": "@regunav/ontology",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo skip",
    "test:schemas": "node conformance/validate-schemas.mjs"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@regunav/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
